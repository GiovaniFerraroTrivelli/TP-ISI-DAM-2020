<resources>
    <!-- Base application theme. -->
    <style name="NoToolbarStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#0C964D</item>
        <item name="colorPrimaryDark">#0C964D</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DefaultToolbarStyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">#0C964D</item>
        <item name="colorPrimaryDark">#0C964D</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- ToolBar -->
    <style name="ToolBarStyle" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
    </style>

</resources>